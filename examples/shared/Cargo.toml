[package]
license = "MIT OR Apache-2.0"
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
bincode = { version = "2.0", features = ["serde"] }
